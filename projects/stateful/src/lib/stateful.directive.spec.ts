import { StatefulDirective } from './stateful.directive';

describe('StatefulDirective', () => {
  it('should create an instance', () => {
    const directive = new StatefulDirective();
    expect(directive).toBeTruthy();
  });
});
